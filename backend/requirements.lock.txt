# FULL pinned transitive dependency closure -- generated from requirements.txt
# via `uv pip freeze`, with unused scikit-learn/scipy/joblib/narwhals/threadpoolctl
# (confirmed unimported anywhere in backend/, see SBOM.md) stripped out.
#
# This is the file to pre-provision wheels from for an air-gapped deployment
# (PRD section 10): download every wheel listed here once, while online, and
# stage them locally before entering the air-gapped environment, e.g.:
#   pip download -r requirements.lock.txt -d ./wheelhouse
#   pip install --no-index --find-links ./wheelhouse -r requirements.lock.txt
#
# Regenerate after any change to requirements.txt:
#   uv pip install -r requirements.txt --python <fresh-venv>
#   uv pip freeze --python <fresh-venv> | sort > requirements.lock.txt
# then re-run the full test suite against the regenerated lock before committing.

annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
click==8.5.0
cryptography==50.0.1
fastapi==0.141.1
filelock==3.32.4
flatbuffers==25.12.19
fsspec==2026.7.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.19
iniconfig==2.3.0
jinja2==3.1.6
markupsafe==3.0.3
ml-dtypes==0.6.0
mpmath==1.3.0
networkx==3.6.1
numpy==2.5.2
onnx==1.22.0
onnxruntime==1.29.0
packaging==26.3
pillow==12.3.0
pluggy==1.6.0
protobuf==7.36.0
pycparser==3.0
pydantic-core==2.46.5
pydantic==2.13.5
pygments==2.21.0
pytest==9.1.1
python-multipart==0.0.32
reportlab==5.0.1
setuptools==84.0.0
starlette==1.6.0
sympy==1.14.0
torch==2.13.0
typing-extensions==4.16.0
typing-inspection==0.4.4
uvicorn==0.52.4
